Numbers = {};

Numbers.degreesToRadians = function ( degrees ) {
    return ( Math.PI / 180 ) * degrees;
};

Numbers.radiansToDegrees = function ( radians ) {
    return radians * ( Math.PI / 180 );
};
